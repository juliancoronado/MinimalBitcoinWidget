<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MinimalBitcoinWidget" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue_500</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/black</item>
        <!-- Customize your theme here. -->
        <item name="backgroundColor">@color/black</item>
        <item name="cardBackgroundColor">@color/card_night</item>
        <item name="textAppearanceBody1">@color/white</item>
        <item name="colorControlNormal">@color/white</item>


    </style>

    <style name="ThemeOverlay.MinimalBitcoinWidget.AppWidgetContainer" parent="">
        <item name="appWidgetBackgroundColor">@color/dark_widget</item>
        <item name="appWidgetTextColor">@color/white</item>
        <item name="colorControlNormal">@color/white</item>
    </style>

</resources>